import React from 'react';
import {ActivityIndicator, SafeAreaView} from 'react-native';

const Home = () => {
  return (
    <SafeAreaView>
      <ActivityIndicator />
    </SafeAreaView>
  );
};

export default Home;
