import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  inputContainer: {
    marginHorizontal: 20,
    marginBottom: 12,
  },
  datePickerContainer: {
    paddingRight: 20,
    paddingBottom: 20,
  },
});

export default style;
