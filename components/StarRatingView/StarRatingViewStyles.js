import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  ratingContainer: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  starRatingContainer: {
    marginBottom: 5,
  },
  ratingText: {
    fontFamily: 'Roboto',
    fontSize: 12,
    marginLeft: 5,
    marginBottom: 5,
  },
  starRating: {
    marginHorizontal: 1,
  },
});

export default style;
