import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  mainContainer: {
    position: 'absolute',
    width: 50,
    right: 6,
    bottom: 30,
    alignItems: 'center',
  },
});

export default style;
