import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  mainContainer: {
    position: 'absolute',
    width: 50,
    right: 6,
    bottom: 65,
    alignItems: 'center',
  },
  iconContainer: {
    marginBottom: 14,
    alignItems: 'center',
  },
  iconText: {
    color: 'white',
    fontSize: 10,
    marginTop: 3,
  },
});

export default style;
