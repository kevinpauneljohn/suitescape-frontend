import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  container: {
    marginBottom: 20,
    alignItems: 'center',
  },
  text: {
    color: 'white',
    fontSize: 10,
    marginTop: 3,
  },
});

export default style;
